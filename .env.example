# RAGarxiv — шаблон окружения. Скопируй в .env и заполни:
#   cp .env.example .env

# Ключ Anthropic — нужен ТОЛЬКО eval-судье RAGAS (src/eval).
# Самому RAG-приложению (src/app) ключ не требуется.
# Получить: https://console.anthropic.com/
ANTHROPIC_API_KEY=
