# Только Qdrant как внешняя зависимость.
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: ragarxiv_db
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_data:
